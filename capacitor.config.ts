import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.whatsgram.project',
  appName: 'whatsgram',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
